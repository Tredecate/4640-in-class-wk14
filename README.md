# 4640-make-up-terraform
This repository contains the code for the Terraform configuration used in the 4640 Make Up lab. The configuration has a few errors that need to be fixed in order for the lab to work properly. These errors are presented in the screenshots below, along with the fixes that were applied to resolve them.

## Error 1
![error1](/.screenshots/error1.png)

fixed default route by adding letter "a" to route_table_id

## Error 2
![error2](/.screenshots/error2.png)
fixed default route by adding letter "a" to route_table_id


## Error 3
![error3](/.screenshots/error3.png)
Fixed argument for subnet id by adding "e" to the `subnet_id` variable in the `main.tf` file.

## Error 4
![error4](/.screenshots/error4.png)
fixed default route by adding letter "a" to route_table_id

fixed web-http ingress rule by adding letter "o" to from_prt


fixed invalid path by removing /module from user_data parameter


## Error 5
![error5](/.screenshots/error5.png)
fixed default route by adding letter "a" to route_table_id

fixed web-http ingress rule by adding letter "o" to from_prt


fixed invalid path by removing /module from user_data parameter


## Error 6
![error6](/.screenshots/error6.png)
fixed default route by adding letter "a" to route_table_id

fixed web-http ingress rule by adding letter "o" to from_prt


fixed invalid path by removing /module from user_data parameter
